@page
@model BookStore.Pages.OrderBooks.CreateModel

@{
    ViewData["Title"] = "Create";
}

<h2>Create</h2>

<h4>OrderBook</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="OrderBook.OrderId" class="control-label"></label>
                <select asp-for="OrderBook.OrderId" class ="form-control" asp-items="ViewBag.OrderId"></select>
            </div>
            <div class="form-group">
                <label asp-for="OrderBook.BookId" class="control-label"></label>
                <select asp-for="OrderBook.BookId" class ="form-control" asp-items="ViewBag.BookId"></select>
            </div>
            <div class="form-group">
                <label asp-for="OrderBook.Count" class="control-label"></label>
                <input asp-for="OrderBook.Count" class="form-control" />
                <span asp-validation-for="OrderBook.Count" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
